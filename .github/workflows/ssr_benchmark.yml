name: 'SSR Benchmark'

on:
  push:
    branches-ignore:
      - main
    paths:
      - '.github/workflows/ssr_benchmark.yml'
      - 'benchmarks/**'
      - 'bin/runSsrBenchmarks.ts'
      - 'bin/ssrBenchmark.ts'
      - 'packages/core/**'
      - 'packages/ssr/**'
  pull_request:
    paths:
      - '.github/workflows/ssr_benchmark.yml'
      - 'benchmarks/**'
      - 'bin/runSsrBenchmarks.ts'
      - 'bin/ssrBenchmark.ts'
      - 'packages/core/**'
      - 'packages/ssr/**'

concurrency:
  group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}

      - name: Ensure PR base commit is available
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          if ! git cat-file -e "$BASE_SHA^{commit}" 2>/dev/null; then
            git fetch --no-tags origin "$BASE_SHA"
          fi

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - name: Install dependencies (head)
        run: bun install --frozen-lockfile

      - name: Run SSR benchmark
        id: benchmark_run
        continue-on-error: true
        run: |
          bun bin/runSsrBenchmarks.ts \
            --base-ref=${{ github.event.pull_request.base.sha }} \
            --runs=25 \
            --warmup=5 \
            --repeat=15 \
            --max-regression-percent=3.0 \
            --max-regression-ms=1.0 \
            --noise-threshold-percent=1.5 \
            --bootstrap-iterations=1000 \
            --bootstrap-seed=0 \
            --fail-on-regression=true \
            --output-dir=/tmp/ssr-benchmark-data \
            --export-markdown=/tmp/ssr-benchmark-report.md \
            --export-json=/tmp/ssr-benchmark-report.json

      - name: Publish step summary
        if: always()
        continue-on-error: true
        run: |
          if [ -f /tmp/ssr-benchmark-report.md ]; then
            tee -a "$GITHUB_STEP_SUMMARY" < /tmp/ssr-benchmark-report.md
          else
            echo "## ⚡ SSR Benchmark" >> "$GITHUB_STEP_SUMMARY"
            echo "⚠️ Benchmark report could not be generated." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Comment on pull request
        if: always() && github.event.pull_request.head.repo.full_name == github.repository
        continue-on-error: true
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('node:fs')
            const reportPath = '/tmp/ssr-benchmark-report.md'
            const report = fs.existsSync(reportPath)
              ? fs.readFileSync(reportPath, 'utf8')
              : '## SSR Benchmark\n\n:warning: Benchmark report could not be generated. Check the workflow logs.'
            const marker = '<!-- ssr-benchmark-report -->'
            const body = `${marker}\n${report}`

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100,
            })
            const existing = comments.find((comment) => comment.body?.includes(marker))

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              })
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              })
            }

      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ssr-benchmark-report-${{ github.run_id }}-${{ github.run_attempt }}
          if-no-files-found: warn
          path: |
            /tmp/ssr-benchmark-report.md
            /tmp/ssr-benchmark-report.json
            /tmp/ssr-benchmark-data/head/*.json
            /tmp/ssr-benchmark-data/base/*.json

      - name: Clean up benchmark worktree
        if: always()
        run: git worktree remove /tmp/ssr-benchmark-data/base-worktree --force || true

      - name: Fail on benchmark regression
        if: always() && steps.benchmark_run.outcome == 'failure'
        run: exit 1
