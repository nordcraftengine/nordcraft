{
  "$schema": "../../schemas/libFormula.schema.json",
  "name": "Parse JSON",
  "description": "Parse a String to JSON. See [JSON.parse on MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/parse) for more information.",
  "arguments": [
    {
      "name": "JSON string",
      "formula": { "type": "value", "value": null },
      "description": "The String to be parsed.",
      "type": { "type": "String" }
    }
  ],
  "output": {
    "description": "The parsed JSON value. If the input is not a valid String, this returns Null.",
    "type": { "type": "Any" }
  }
}
