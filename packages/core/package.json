{
  "name": "@nordcraft/core",
  "license": "Apache-2.0",
  "type": "module",
  "homepage": "https://github.com/nordcraftengine/nordcraft",
  "scripts": {
    "build": "tsgo",
    "npm-publish": "bun run build && bun publish --access public",
    "typecheck": "tsgo --noEmit",
    "watch": "tsgo -w",
    "generateTypes": "bun run bin/generateSchemas.ts && ajv compile --allow-union-types -s src/component/schemas/component.schema.json -o src/component/schemas/component.js",
    "validate": "bun run bin/validate.ts",
    "postinstall": "./bin/postInstall.sh"
  },
  "files": [
    "dist",
    "src"
  ],
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20251208.1",
    "ajv-cli": "5.0.0",
    "ts-json-schema-generator": "2.4.0"
  },
  "main": "dist/index.js"
}
